% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/find.pitch.R
\name{find.pitch}
\alias{find.pitch}
\title{Find 3D pitches that project to a known 2D pitch (given yaw and view elevation)}
\usage{
find.pitch(
  pitch2d,
  view_elevation,
  yaws = NULL,
  pitches = NULL,
  round2keep = 0,
  plot = FALSE
)
}
\arguments{
\item{pitch2d}{Numeric scalar: observed 2D pitch, in degrees, in the interval (-180, 180];
e.g., from \code{\link[=pitch2d.from.xy]{pitch2d.from.xy()}}.}

\item{view_elevation}{Numeric scalar: angle between camera and object, in degrees, in the
interval [-90, 90]. Convention: \code{-90} = seen from straight below, \code{0} = eye level,
\code{90} = seen from straight above.}

\item{yaws}{Optional numeric vector of one known or multiple candidate yaw angles, in degrees, in the
interval (-180, 180]. Convention: \code{0} = pointed right, \code{90} = pointed straight away,
\code{-90} = pointed straight toward, \code{180} = pointed left. If \code{NULL} (default), a grid
comprehending all possible yaws (\code{seq(-179.99, 180, 0.1)}) is used.}

\item{pitches}{Optional numeric vector of candidate 3D pitches used to filter the results,
in degrees, in the interval [-90, 90]. Convention: \code{90} = pointed up, \code{0} = horizontally
aligned, \code{-90} = pointed down. If \code{NULL} (default) all computed pitches are returned.}

\item{round2keep}{Integer scalar: number of decimal places to pass to \code{\link[=round]{round()}} when
comparing equality between computed and candidate pitches/yaws. Default is \code{0}, i.e.,
equality is checked after rounding computed and candidate values to integers.}

\item{plot}{Logical scalar. If \code{TRUE}, the function plots the full continuous pitch-by-yaw
curve, overlays lines for the selected \code{yaws}/\code{pitches}, and points for the returned
combinations.}
}
\value{
A \code{data.frame} with two columns:
\describe{
\item{yaws}{numeric: the (input) yaw values, in degrees, retained after filtering}
\item{pitches}{numeric: the corresponding computed 3D pitches, in degrees,
from -90 (pointed down), through 0 (horizontally aligned), to 90 (pointed up)}
}
If no (yaw, pitch) matches are found the returned data.frame has zero rows.
}
\description{
Given a measured 2D pitch and one or more candidate yaw orientations, compute the
corresponding 3D pitch angles that would produce that projected 2D pitch at a given view
elevation.
}
\details{
In practice \code{\link[=find.pitch]{find.pitch()}} finds which solutions of \code{\link[=pitch2d.from.3d]{pitch2d.from.3d()}} reproduce \code{pitch2d}.

Computationally, \code{\link[=pitch2d.from.3d]{pitch2d.from.3d()}} constructs the full rotation
matrix \eqn{R} using \code{\link[=rotate3d]{rotate3d()}} and computes the projected 2D pitch \eqn{p_{2}} as
\deqn{p_{2} = \operatorname{atan2}\!\big(R_{2,1},\,R_{1,1}\big)}

By inversing this relation, we find which 3D pitch (\eqn{p_{3}}) projects \code{pitch2d}
(\eqn{p_{2}}) from \code{view_elevation} (\eqn{r_{3}}) given a candidate/known \code{yaw}
(\eqn{y_{3}}):
\deqn{p_{3} = \operatorname{atan2}(\tan(p_{2})\cos(y_{3}) - \sin(r_{3})\sin(y_{3}), \cos(r_{3}))}

This expression is equivalent to the code inside \code{\link[=find.pitch]{find.pitch()}}.
}
\examples{
# pitches that projects to 10° seen from 15° below
find.pitch(10, view_elevation = -15, plot = TRUE)

# similar to above, but now given known yaw of 30°
find.pitch(10, view_elevation = -15, yaws = 30, plot = TRUE)

# similar to above, but now given range of candidate yaws instead (20 to 45°)
find.pitch(10, view_elevation = -15, yaws = seq(20, 45, 0.1), plot = TRUE)

# similar to above, but now restricting (with integer rounding) to range of candidate
# pitches 0 to 15°
find.pitch(10, view_elevation = -15, yaws = seq(20, 45, 0.1), pitches = seq(0, 15, 0.1),
           plot = TRUE)

}
\seealso{
\code{\link[=pitch2d.from.xy]{pitch2d.from.xy()}}, \code{\link[=find.yaw]{find.yaw()}}, \code{\link[=pitch2d.from.3d]{pitch2d.from.3d()}}
}
