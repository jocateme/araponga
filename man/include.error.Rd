% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/include.error.R
\name{lookup.error}
\alias{lookup.error}
\alias{include.error}
\alias{pitch2d.w.error}
\title{Lookup and incorporate expected error in pitch or yaw estimation}
\usage{
lookup.error(
  raw = NULL,
  pitch2d = NULL,
  view_elevation = NULL,
  yaws = NULL,
  pitches = NULL,
  view_elevation_error = c("0", "2.5", "5", "7.5"),
  label_error = c("0", "2", "5", "8"),
  label_nsim = 100
)

include.error(
  raw,
  view_elevation_error = c("0", "2.5", "5", "7.5"),
  label_error = c("0", "2", "5", "8"),
  label_nsim = 100,
  plot = FALSE
)

pitch2d.w.error(pitch2d, label_error = c("0", "2", "5", "8"), label_nsim = 100)
}
\arguments{
\item{raw}{Optional. The object returned by \code{\link[=find.pitch]{find.pitch()}} or \code{\link[=find.yaw]{find.yaw()}}. If supplied,
overrides the arguments below.}

\item{pitch2d}{Numeric scalar: observed 2D pitch as returned by \code{\link[=pitch2d.from.xy]{pitch2d.from.xy()}}, in degrees in
the interval (-180, 180]. Ignored if \code{raw} is provided.}

\item{view_elevation}{Numeric scalar: angle between camera and object, in degrees, in the
interval [-90, 90]. Convention: \code{-90} = seen from straight below, \code{0} = eye level,
\code{90} = seen from straight above. Ignored if \code{raw} is provided.}

\item{yaws}{Optional numeric vector of candidate yaw angles, in degrees, in the
interval (-180, 180]. Convention: \code{0} = pointed right, \code{90} = pointed straight away,
\code{-90} = pointed straight toward, \code{180} = pointed left. If \code{NULL} (default), a grid
comprehending all simulated yaws is used. Ignored if \code{raw} is provided.}

\item{pitches}{Optional numeric vector of candidate pitch angles, in degrees, in the interval
[-90, 90]. Convention: \code{90} = pointed up, \code{0} = horizontally aligned, \code{-90} = pointed down.
If \code{NULL} (default), a grid comprehending all simulated pitches is used. Ignored if \code{raw} is
provided.}

\item{view_elevation_error}{Character choice specifying the error (± degrees) in view elevation
measurement. One of \code{"0"}, \code{"2.5"}, \code{"5"}, or \code{"7.5"}.}

\item{label_error}{Character choice specifying the error (± pixels) in landmark
labeling. One of \code{"0"}, \code{"2"}, \code{"5"}, or \code{"8"}.}

\item{label_nsim}{Integer scalar (default 100). Number of Monte Carlo draws passed internally to
\code{pitch2d.w.error()} to simulate error in pitch2d calculation when \code{label_error != "0"}.}

\item{plot}{Logical scalar. \code{TRUE} plots original and expanded sets of angles side by side.}
}
\value{
\code{lookup.error()} returns a named list with two elements:
\describe{
\item{\code{pitch}}{a list of maximum errors in pitch per named source of error.}
\item{\code{yaw}}{a list of maximum errors in yaw per named source of error.}
}

\code{include.error()} returns a numeric vector with the expanded candidate angles (pitches or yaws)
after incorporating the maximum error retrieved by \code{lookup.error()$pitch} or \code{lookup.error()$yaw}
with a 0.01 resolution. The maximum error is stored as vector in the attribute \code{error.included}.
}
\description{
Estimate expected pitch and yaw errors from simulation results with \code{lookup.error()} and expand
candidate angle sets to incorporate those errors with \code{include.error()}.

These functions use a simulation dataset (\code{sim_data}) included with the package that quantifies
the discrepancy between true 3D angles and the angles recovered by the projection methods
implemented in \code{\link[=find.pitch]{find.pitch()}} and \code{\link[=find.yaw]{find.yaw()}}. The simulations can account for three sources of
error: inherent method error, mismeasurement of view elevation, and landmark labeling inaccuracy.

\code{lookup.error()} retrieves the expected maximum error from any of these error sources, while
\code{include.error()} both retrieves the maximum error and expands a set of candidate pitch or yaw
angles returned by \code{\link[=find.pitch]{find.pitch()}}/\code{\link[=find.yaw]{find.yaw()}} so that all values within the expected error
range are included. \code{pitch2d.w.error()} simulates the effect of labeling inaccuracy on \code{pitch2d}
calculation.
}
\seealso{
\code{\link[=find.pitch]{find.pitch()}}, \code{\link[=find.yaw]{find.yaw()}}, \code{\link[=pitch2d.from.xy]{pitch2d.from.xy()}}
}
