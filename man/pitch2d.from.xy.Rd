% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pitch2d.from.xy.R
\name{pitch2d.from.xy}
\alias{pitch2d.from.xy}
\title{Calculate projected 2D pitch from two landmarks}
\usage{
pitch2d.from.xy(x_tip, y_tip, x_base, y_base, plot = FALSE)
}
\arguments{
\item{x_tip, x_base}{Numeric scalars or vectors: image x-coordinate(s) of tip and base of object, with
x increasing right.}

\item{y_tip, y_base}{Numeric scalars or vectors: image y-coordinate(s) of the tip and base of objects,
with y increasing up.}

\item{plot}{Logical scalar. \code{TRUE} draws a diagnostic plot with base-tip segment and calculated 2D
pitch angle. Plotting is only supported for single base-tip pair.}
}
\value{
Numeric vector of 2D pitch angles, in degrees, in the interval (-180°, 180°].
Convention: \code{0} points right, \code{90} points up, \code{-90} points down, \code{180} points left.
}
\description{
Compute the 2D pitch angle defined by two landmarks: a tip point and a base point.
}
\details{
The calculation uses the two-landmark vector
\deqn{(dx,dy)=(x_{tip}-x_{base}, y_{tip}-y_{base})}
and returns
\deqn{p_{2} = \operatorname{atan2}(dy, dx).}
}
\examples{
# scalar examples (plots)
pitch2d.from.xy(1, 0, 0, 0, plot = TRUE) # pointed right -> 0°
pitch2d.from.xy(-1, 0, 0, 0, plot = TRUE) # pointed left -> 180°
pitch2d.from.xy(0, 1, 0, 0, plot = TRUE) # pointed up -> 90°
pitch2d.from.xy(0, -1, 0, 0, plot = TRUE) # pointed down -> -90°

# vectorised usage (no plot)
x_tips  <- c(1, 0, -1)
y_tips  <- c(0, 1, 0)
x_bases <- c(0, 0, 0)
y_bases <- c(0, 0, 0)
pitch2d.from.xy(x_tips, y_tips, x_bases, y_bases)

}
\seealso{
\code{\link[=pitch2d.w.error]{pitch2d.w.error()}}, \code{\link[=pitch2d.from.3d]{pitch2d.from.3d()}}
}
